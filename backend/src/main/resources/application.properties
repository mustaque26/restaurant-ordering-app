spring.application.name=restaurant-backend
server.port=8080

spring.datasource.url=jdbc:h2:mem:restaurantdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.defer-datasource-initialization=true

app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173

# Email configuration - two SMTP configs: franzzo (system) and sales (Axinq)
# 1) System (Franzzo) - used for order confirmations and OTPs
spring.mail.franzzo.host=smtp.gmail.com
spring.mail.franzzo.port=587
spring.mail.franzzo.username=franzzo057@gmail.com
spring.mail.franzzo.password=bakn suvg zaoc wslt

# 2) Sales (Axinq) - used for tenant onboarding and sales emails
spring.mail.sales.host=smtp.gmail.com
spring.mail.sales.port=587
spring.mail.sales.username=consulting@axinq.com
spring.mail.sales.password=zcfd uihj fpuv quze

# Default From address (used where appropriate)
spring.mail.from=${spring.mail.sales.username:consulting@axinq.com}

# Common mail properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8

# WhatsApp sender number (used as default "from")
app.whatsapp.from=+919270461728

# To enable sending WhatsApp via Twilio, set these environment variables:
# TWILIO_ACCOUNT_SID and TWILIO_AUTH_TOKEN

# Optional: admin/store number to receive a copy of WhatsApp order notifications
# Set to empty to disable admin copy. Example: app.whatsapp.admin=+91987xxxxxxx
app.whatsapp.admin=

# Toggle to enable/disable WhatsApp sending from server; set to false to disable globally
app.whatsapp.enabled=true
